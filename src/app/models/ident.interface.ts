export interface IIdent {
  identId: string;
  name: string;
  description: string;
  lenMax: number;
  lenMin: number;
  onlyDigits: number;
  testing: number;
  // translating: object; //TODO: pendiente agregar objecto
}
